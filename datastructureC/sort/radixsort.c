#include<stdio.h>
#include<stdlib.h>
int main();
{
    
  //radix(A,d)
  //for i to d digits
  //use an sort for each digit
  //if use count sort =>(not in place) for more place
  //if use insertion sort =>(inplace) however run time nor for O(n)

}